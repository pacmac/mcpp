"""
Deprecated template.

This wrapper now enforces directory modules only: `tools/<module>/main.py`.
Use `tools/_template_pkg/` as the copy-paste starter instead.

This file remains only as a pointer and is ignored by the wrapper.
"""

from __future__ import annotations
